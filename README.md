"# Aplicación Android Nativa - LogiMind SILOG WMS APK\n\nEsta carpeta contiene el código fuente nativo de la aplicación de almacén para operarios, estructurada como un proyecto de aplicación Android de Gradle compilable a un archivo ejecutable **`.apk`**.\n\nEstá diseñada para ejecutarse en terminales portátiles industriales **Zebra (TC57, TC52, MC3300)** y teléfonos Android normales.\n\n## Requisitos de Entorno\n\n- **JDK 17** instalado y configurado en el sistema.\n- **Android SDK** (API Level 34) instalado.\n- Dispositivo móvil Android o colector con Android 8.0 (API 26) o superior.\n\n## Instrucciones para Compilar y Generar el APK\n\n1. Abra una terminal o consola de comandos en esta carpeta (`c:/DESARROLLOS/APP CONTROL DIETA/app_android_apk`).\n2. Descargue e instale dependencias compilando el proyecto en modo depuración (debug) con el Gradle Wrapper:\n   ```bash\n   # En Windows:\n   gradlew assembleDebug\n\n   # En macOS/Linux:\n   ./gradlew assembleDebug\n   ```\n3. Una vez finalizado el proceso de construcción, el archivo APK generado se guardará en la siguiente ruta:\n   `app_android_apk/build/outputs/apk/debug/app_android_apk-debug.apk`\n\n## Cómo instalar el APK en un Colector Zebra\n\n1. Conecte su PDA Zebra a la computadora mediante cable USB.\n2. Asegúrese de tener activada la **Depuración por USB** (Developer Options) en el dispositivo.\n3. Ejecute el comando de instalación usando la herramienta ADB (Android Debug Bridge):\n   ```bash\n   adb install build/outputs/apk/debug/app_android_apk-debug.apk\n   ```\n\n## Configuración del Lector Láser Físico (Zebra DataWedge)\n\nPara que el lector láser de código de barras físico del colector se comunique con la app, debe configurar el perfil de **DataWedge** en la PDA:\n\n1. Abra la aplicación **DataWedge** en el menú de la PDA Zebra.\n2. Cree un nuevo perfil llamado `LogiMindWMS`.\n3. En la sección **Associated Apps**, asocie el paquete `com.logimind.silog.wms` y la actividad `*` (todas).\n4. En **Intent Output**:\n   - Marque
<truncated 147 bytes>